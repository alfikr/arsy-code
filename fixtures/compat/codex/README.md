# Codex fixture

Working directory: `services/api`. The nested `AGENTS.override.md` is more specific than the root instructions. Project TOML selects a model, approval policy, workspace sandbox, and stdio MCP server. These are requests constrained by enterprise/user policy, never grants.
