# Standalone AGENTS fixture

Working directory: `services/payments`. The root and `services` instructions compose in root-to-leaf order; the nearest `AGENTS.override.md` is the most specific source. This fixture carries no executable configuration.
