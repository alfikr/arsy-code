# OMP fixture

Working directory: `packages/api`. OMP's nearest non-empty native `.omp` directory owns project discovery, so `packages/api/.omp/AGENTS.md` is selected and the root `.omp/AGENTS.md` is not composed with it. Native YAML, skill, agent, extension, and MCP declarations are translated as data; TypeScript is quarantined.
