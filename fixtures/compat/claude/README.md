# Claude Code full-surface fixture

Working directory: `services/payments`. Project instructions compose with the nested file. `.claude/settings.local.json` overrides the project model while project permission and hook declarations remain. The importer parses skills, agents, plugin declarations, and MCP as data; it does not execute the hook, plugin, or MCP command.
